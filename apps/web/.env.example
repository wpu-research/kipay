# API (tarayıcıdan erişim için — client-side fetch)
NEXT_PUBLIC_API_URL=http://localhost:3001

# API (sunucu tarafı SSR fetch için — tarayıcıya açılmaz)
# Production'da iç ağ adresi kullanın (örn. http://api:3001)
API_INTERNAL_URL=http://localhost:3001

# App
NODE_ENV=development
